# Optional deployment patch for `dsh --profile data-aid`.
# The shipped @deepseek-ai/dsh-data-aid bundle already owns the complete closed
# composition and reads DATA_AID_QUERY_* from the environment. Keep this layer
# empty unless the deployment must override an existing safe row by id.
[]
