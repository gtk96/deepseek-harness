# 任务 9 评审 — 实现 DSH `dataQuery` Service Definition

**状态**：approved
**评审轮次**：r1
**实现范围**：分支 `feat/controlled-data-query-mvp` 上相对最近相关提交 `a68ecac9d4`、`10cd1552de` 的当前未提交实现；逐项检查 `packages/identity/data-query/README.i18n.yaml`、`README.md`、`README.zh.md`、`package.json`、`src/index.ts`、`src/types.ts`、未跟踪的新增 `src/error.ts`、`src/invariant.ts`、`tests/data-query.spec.ts`、`tsconfig.json`。同时检查该包在 `tsconfig.host.json`、类型等价清单、配置目录、运行时依赖闭包和构建产物中的登记。未把 Task 10 HTTP Provider、Task 11 Tool Consumer/Profile 及其它工作树改动纳入实现评审。

## 验收核验（对照 requirements.md）

| 验收标准 | 结果 | 证据 |
|---|---|---|
| Task 9.1：定义 `DataQueryRequest`、`DataQueryResult`、`DataQueryProvider` 和运行时服务 | 通过 | `src/types.ts` 将纯语义请求、可信 `DataQueryContext`、五字段完整结果和 Provider 接口分开；`src/index.ts` 默认导出 `DataQueryRuntime`。定向 `tsc --noEmit`、9 个单元测试和 tsdown 无写入构建均通过。 |
| Task 9.1：Provider 显式选择，或仅在唯一可用时自动选择；缺失、不可用、歧义和重复注册均失败 | 通过 | `resolveProvider()` 每次调用重新读取注册表；显式路径区分 missing/unavailable，自动路径区分 zero/ambiguous，歧义消息按 id 排序；五个稳定 runtime 错误码由 `src/error.ts` 导出。单测覆盖全部路径及注册顺序变化，9/9 通过。 |
| Task 9.1：registry contribution 返回 disposer，并验证 HMR 清理 | 通过 | `registerProvider()` 通过 `ctx.effect()` 注册并返回 disposer；单测分别验证显式 disposer 和贡献 fiber dispose 后查询失败。`verify-package-invariants` 与 built invariant Loader 检查通过。 |
| Task 9.2：README 说明配置、失败、限制、扩展点和 Model Experience | 通过 | 中英文 README 均包含 `Configuration`、`Failures`、`Extension point`、`Model Experience`、KV Cache 和 `Known Limitations and Deferred Work`；双语 pairing、Model Experience、limitations 门禁通过。 |
| Task 9.2：invariant 验证可观察关系，或给出包特定空理由 | 通过 | `src/invariant.ts` 明确说明 provider registration 是私有 effect、没有独立事件/投影，查询直接读取权威 map；符合仓库允许空 companion 的规则。源码门禁报告 232 个 companion 通过，构建产物 Loader 门禁同样 232 个通过。 |
| Task 9.2：公开 Service Definition 与错误的 README/JSDoc 同步 | 通过 | `src/index.ts`、`src/types.ts`、`src/error.ts` 和 `src/invariant.ts` 的公开导出、参数、返回值和失败语义均有 JSDoc；全仓 JSDoc 门禁只报告 `packages/mcp/mcp-client/src/mcp-clients.ts` 两条无关违规，没有 Task 9 路径违规。 |
| Task 9.3：覆盖 Provider 选择、重复注册、调用取消、结果边界和 disposal | 通过 | `tests/data-query.spec.ts` 的 9 个测试覆盖显式/自动选择、零/多候选、稳定错误码、非法与重复 id、`AbortSignal`/可信上下文原样透传、请求/结果键边界、品牌 id 不可互换、disposer 和 HMR 清理；实际结果 1 file / 9 tests passed。 |
| Requirement 3.2：模型工具参数只含语义字段，不含 SQL、物理名、身份、范围、地址、超时、凭据或 Provider 选择 | 通过（Service Definition 范围） | `DataQueryRequest` 仅含 design 6.2 所列语义字段；类型测试以 `@ts-expect-error` 覆盖 roles、scope、project、table、physicalField、endpoint、baseURL、timeout、credential、provider，并用类型断言确认无 principal/conversation/turn/assertion/sql/jobId。模型 JSON 的运行时解析由 Task 11 Consumer 负责，不属于本包。 |
| Requirement 3.3：可信主体、conversationId、turnId 不来自模型参数 | 通过（Service Definition 范围） | 三者只存在于独立 `DataQueryContext`；conversation/turn 使用不同 `Branded` 类型，主体使用 `GkUserId`；`query(request, context, signal?)` 分参透传。Consumer 如何取得可信轮次状态属于 Task 11，本轮未证实。 |
| Requirements 3.5–3.6：只暴露完整受限结果，部分/截断或非法结果不得成功 | 通过（Service Definition 类型范围） | `DataQueryResult` 精确声明 columns、rows、rowCount、`complete: true`、`truncated: false`，无 SQL/jobId；Provider 返回该类型。HTTP 非可信响应的运行时拒绝由 Task 10 Provider 负责，本轮未证实，也未把它误记为本包通过。 |
| Requirements 3.1、3.7、9.5：最终唯一模型工具、模型可见日志和专用 Profile 禁止原始 MCP/直接查询工具 | 未证实（明确不在本轮实现范围） | 本包是 Service Definition，不注册模型工具、不写模型日志且无 MCP 依赖；最终装配与 transcript 是 Task 11 Consumer/Profile 的验收面。README 正确标为经 Consumer 间接影响。 |
| Requirement 3.4：短期断言、固定 audience、随机 jti | 未证实（明确不在本轮实现范围） | Service Definition 明确不承载 assertion；签发和 HTTP 传输属于 Task 10 Provider。 |
| Requirement 9.6：发布产物使用构建代码与已声明依赖 | 通过（目标包范围） | package tsconfig 定向无写入类型检查通过；tsdown `-F @deepseek-ai/dsh-data-query --no-write` 构建通过；plain Node 能从现有 `lib/index.js`/`lib/invariant.js` 加载 default、`DataQueryError`、`invariant.apply`；runtime closure、package paths、config catalog、publint 均完成。publint 仅给出仓库既有 `./src/*` 未打包 warning。 |

## 逐文件结论（对照 design.md）

| 文件 | 保留/修改/缺失 | 依据 |
|---|---|---|
| `packages/identity/data-query/src/index.ts` | 保留 | Service 默认导出正确；配置可显式选择；每次调用确定性解析唯一可用 Provider；注册为 effect；错误分支和 JSDoc 完整。 |
| `packages/identity/data-query/src/types.ts` | 保留 | 与 design 6.2/6.3 的语义请求和五字段响应一致；可信上下文独立，opaque id 品牌化；无 Provider、传输或物理执行字段泄入 request/result。 |
| `packages/identity/data-query/src/error.ts` | 保留（新增、当前未跟踪） | 基于 `HarnessError` 提供开放式错误码，并导出 Service Runtime 所需的五个稳定 Provider-neutral code；允许 Provider 扩展 code。提交实现时必须确保该新增文件被纳入变更。 |
| `packages/identity/data-query/src/invariant.ts` | 保留 | 空 installer 有包特定且符合权威关系规则的理由，注册 disposer 正确返回。 |
| `packages/identity/data-query/tests/data-query.spec.ts` | 保留 | 行为覆盖与 Task 9.3 一致，包含 HMR/disposer、取消透传、稳定错误码、类型负例和结果键边界。 |
| `packages/identity/data-query/README.md` | 保留 | 配置、API、失败、扩展点、模型影响、KV Cache 和限制完整，并准确说明 Consumer/Provider 责任。 |
| `packages/identity/data-query/README.zh.md` | 保留 | 与英文语义一致，pairing 门禁通过。 |
| `packages/identity/data-query/README.i18n.yaml` | 保留 | 两侧摘要与当前 README 一致。 |
| `packages/identity/data-query/package.json` | 保留 | 新增 brand 与 llm 的 peer/dev 声明匹配类型/运行时引用；默认与 invariant exports、files 清单保持正确。 |
| `packages/identity/data-query/tsconfig.json` | 保留 | 新增 workspace 引用覆盖 brand、authenticated-principal、llm 和 invariants，并已登记于 host aggregate。 |
| design 所需 Task 9 包文件 | 无缺失 | Service Definition、types、errors、invariant、README、tests、package manifest 和 tsconfig 均存在；Task 10 Provider 与 Task 11 Consumer 是独立角色，不在本轮文件结论中。 |

## 发现（按 P1/P2/P3）

### P1 阻断

无。

### P2 需修

无。

### P3 建议

无。

非 Task 9 阻断说明：全仓 `verify-export-jsdoc`、workspace constraints 和 `verify-type-equiv` 当前分别被 MCP Client、两个缺少 package.json 的 client 目录、Typert 文档漂移阻断；命令输出均未指向 `packages/identity/data-query`。这些结果说明全仓门禁并非全绿，但不能归因于本轮 Service Definition。`publint` 的 `./src/*` warning 也来自该包变更前已存在的导出约定。

## 证据清单

- `git status --short; git diff -- packages/identity/data-query; git diff --cached -- packages/identity/data-query`：退出 0；确认 9 个已跟踪文件修改和新增未跟踪 `src/error.ts`，无 staged diff；完整审阅 diff。
- `git log -8 --oneline --decorate -- packages/identity/data-query`：退出 0；最近相关基线为 `a68ecac9d4 fix: bump new identity packages...`、`10cd1552de feat: data-query capability packages...`。
- 首轮四个 `pnpm exec ...` 命令均在工具启动前退出 1：`pnpm` 非 TTY 依赖状态检查尝试 production install，报 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`；随后改为直接调用已安装的 `node_modules/.bin/*.CMD`，没有安装或修改依赖。
- `.\node_modules\.bin\vitest.CMD run packages/identity/data-query/tests/data-query.spec.ts`：退出 0；1 个测试文件、9 个测试全部通过，耗时 888ms。
- `.\node_modules\.bin\tsc.CMD -p packages/identity/data-query/tsconfig.json --noEmit --tsBuildInfoFile NUL --pretty false`：退出 0，无诊断、无工作树输出。
- LSP `get_diagnostics`：`src/index.ts` 与 `tests/data-query.spec.ts` 均为 `No diagnostics`。
- `.\node_modules\.bin\oxlint.CMD packages/identity/data-query/src packages/identity/data-query/tests`：退出 0；5 files，0 warnings，0 errors。
- `.\node_modules\.bin\tsdown.CMD -F @deepseek-ai/dsh-data-query --no-write`：退出 0；目标 `es2024`，Build complete in 27ms，未写入工作树。
- `.\node_modules\.bin\tsx.CMD scripts/verify-package-invariants.ts`：退出 0；232 个 hand-owned companion 全部符合。
- `.\node_modules\.bin\tsx.CMD scripts/verify-built-package-invariants.mjs`：退出 0；232 个 compiled companion 通过 plain-Node Loader 检查。
- `.\node_modules\.bin\tsx.CMD scripts/verify-package-readme-model-experience.ts`：退出 0；232 README 全部符合。
- `.\node_modules\.bin\tsx.CMD scripts/verify-package-readme-limitations.ts`：退出 0；232 README 全部符合。
- `.\node_modules\.bin\tsx.CMD scripts/verify-translation-pairing.ts packages/identity/data-query/README.md`：退出 0；1 个指定 pair 一致。
- `.\node_modules\.bin\tsx.CMD scripts/verify-runtime-closure.ts`：退出 0；6 presets / 122 workspace packages 的运行时依赖闭包通过。
- `.\node_modules\.bin\tsx.CMD scripts/gen-config-catalog.ts --check`：退出 0；`docs/config-catalog.md` up to date。
- `.\node_modules\.bin\tsx.CMD scripts/verify-package-paths.ts`：退出 0；4170 个路径引用均可解析。
- `.\node_modules\.bin\publint.CMD packages/identity/data-query`：退出 0；lint 完成；仅 warning 为 `exports["./src/*"]` 在打包文件中无匹配。
- `node --input-type=module -e "...import('./packages/identity/data-query/lib/index.js')..."`：退出 0；输出 `built ESM smoke: default, DataQueryError, invariant.apply OK`。
- `.\node_modules\.bin\tsx.CMD scripts/verify-export-jsdoc.ts`：退出 1；仅有 2 条 `packages/mcp/mcp-client/src/mcp-clients.ts:70` 违规，输出中无 Task 9 文件。
- `.\node_modules\.bin\tsx.CMD scripts/check-workspace-constraints.ts`：退出 1；仅报告 `packages/mcp/mcp-client/package.json` files 清单和 `packages/client/schema-form`、`packages/client/web-react` 缺 package.json，均不在本轮范围。
- `.\node_modules\.bin\tsx.CMD scripts/verify-type-equiv.ts`：退出 1；仅报告 `docs/subsystems/typert.md` 对 `packages/api/gateway/src/types.ts` 的 3 处漂移；403 个 primary/403 个 derivative 已检查，输出中无 data-query 漂移。
- `git diff --check -- packages/identity/data-query`：退出 0，无空白错误。随后 `git status --short -- packages/identity/data-query` 仍只显示评审前的 9 个修改和新增 `src/error.ts`，验证命令未改写实现。

## 待办

- 无 Task 9 实现修订项。
- 交接/提交时确认未跟踪的 `packages/identity/data-query/src/error.ts` 被纳入实现变更。
- Task 10/11 的断言、HTTP 非可信响应校验、唯一模型工具、会话日志与最终 Profile 继续在各自任务评审中验证；本报告不替代这些验收。
