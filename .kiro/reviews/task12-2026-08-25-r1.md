# 任务 12 评审 — 实现 dic-fe 智能问数页面

**状态**：needs-changes
**评审轮次**：r1
**实现范围**：独立仓库 `dic-fe/` 当前未提交工作树；检查了 `src/api/data-query/**`、`src/modules/data-query/**`、`src/views/data-query/**`、`src/router/modules/data-query.ts`、`src/router/__tests__/data-query.spec.ts`、`src/api/request.ts`、`src/router/index.ts`、`src/layouts/MainLayout.vue`、`scripts/scan-data-query-chunks.mjs` 及其测试、`package.json`、`vite.config.ts`、各 `.env.*`、`README.md`、`Dockerfile`、`nginx.conf`、`.dockerignore`、`context.md`；并以只读方式核对 `dic-be` 的浏览器接口 schema、统一响应和路由测试。最近相关提交搜索只命中 stash 引用，故本轮实现范围按当前未提交 diff/untracked 文件确定。容器文件只作为 Task 12 构建/产物证据检查，不据此宣称 Task 14 部署完成。

## 验收核验（对照 requirements.md）

| 验收标准 | 结果 | 证据 |
|---|---|---|
| 12.1 / 需求 1.1、1.2：浏览器不提交身份且只访问 dic-be | 通过 | `src/api/data-query/index.ts:33-44` 三个接口全部复用 `@/api/request`，源码限定扫描未发现 data-query 发送 `userId`、`principal`、角色、DSH/Broker 地址、SQL 或凭据；`.env.production`/`.env.test` 只配置现有 dic-be API 前缀。`dic-be/tests/test_data_query_router.py` 的真实路由测试也证明身份来自鉴权中间件。 |
| 12.1：共享 Axios 身份注入与 401 处理 | 通过 | `src/api/request.ts:1-13,34` 的共享 `createAxios` 实例负责 `gk-service-user`/Bearer 注入、HTTP/包体 401 登录弹窗和重试；data-query API 没有另建 Axios。 |
| 12.1 / 需求 2.1–2.2：会话、轮次、状态接口且创建轮次 body 仅有 `question` | 通过 | `src/api/data-query/index.ts:33-44` 封装 POST 会话、POST 轮次、GET 轮次；`src/api/data-query/__tests__/index.spec.ts` 断言空会话 body、URL ID 编码、取消 signal，且轮次 body 的键精确为 `['question']`。核对 `dic-be/app/data_query/interfaces/schemas.py` 后 URL、字段和六态类型一致。 |
| 12.2：独立 `service/data-query` 路由和菜单元数据 | 通过 | `src/router/modules/data-query.ts:6-13` 注册服务层入口和 `l1/l2/l2Label/title/order`；`src/router/__tests__/data-query.spec.ts:5-20` 验证按名称解析到 `/service/data-query`、动态页面和可见菜单元数据。 |
| 12.2 / 需求 2.4–2.6：消息、排队/运行、结果表格、最终回答和终态 UI | 通过 | `Conversation.vue` 组合问题输入和轮次列表；`DataQueryTurnCard.vue:40-45` 展示受控表格及最终回答；状态常量覆盖 `queued/running/succeeded/denied/failed/timed_out`。组件测试验证所有状态、成功表格/回答、空问题和 Vue 文本转义。 |
| 12.3 / 需求 2.5、2.7：有界轮询及终态停止 | 通过 | `constants/index.ts:5-7` 固定 60 次上限并声明四个终态；`useDataQueryConversation.ts:91-114,182-187` 在达到上限或任何终态时停止。测试覆盖 queued→running→succeeded、全部四个终态和达到最大次数。无流式路径。 |
| 12.3：离开页面、取消和卸载清理 | 通过 | `useDataQueryConversation.ts:55,201-219` 清 timer、Abort 在途请求并以版本号拒绝旧回写；`Conversation.vue:19-20` 同时在路由离开和组件卸载 dispose。composable 测试覆盖用户停止、销毁和旧响应，页面测试覆盖路由离开后不再 GET。 |
| 需求 2.3、8.2、8.5：归属错误、权限拒绝及内部细节安全 | 通过 | `DQ_RESOURCE_NOT_FOUND` 使用统一“不存在或无权访问”文案，`DQ_POLICY_DENIED` 显示权限不足和申请授权提示；异常对象从不直接进入响应式展示。测试以含 SQL/Authorization/stack 的异常验证只显示固定安全文案，组件测试验证回答和单元格按文本转义。 |
| 需求 8.6：超能力问法说明限制并建议单数据集汇总问法 | 通过 | `constants/index.ts:20,32` 对 `DQ_SEMANTIC_INVALID` 显示“当前问法暂不支持，请尝试单个数据集的汇总查询”，定向测试覆盖。 |
| 12.3：权限不足、能力不支持、超时和系统失败分类准确 | 未过 | `src/modules/data-query/constants/index.ts:33` 把 `DQ_COMPILATION_FAILED` 映射为用户问法不支持；但 `design.md:376` 定义其含义为“治理映射无法安全编译”，处置为“修复治理记录”。这是平台/治理系统失败，不是用户可通过改问法解决的能力限制。详见 P2-1。 |
| 12.4：API body、轮询清理、所有终态、路由和构建测试 | 通过（有一项分类测试缺口） | 定向 Vitest 实跑 7 files / 36 tests 全通过；覆盖 API、轮询、组件、页面、路由、扫描脚本和容器静态检查。但当前表驱动错误映射测试未包含 `DQ_COMPILATION_FAILED`，因此没有发现 P2-1。 |
| 需求 1.7、10.8 的 dic-fe 代码/构建部分 | 通过；部署后健康检查未证实 | `pnpm run type-check`、scoped ESLint、默认 production build 均成功；构建生成 manifest 与 `Conversation-BddcG8Nv.js`，内置扫描和独立复扫均零命中。实际部署和 `/healthz` 只做了静态配置测试，未启动容器或访问部署环境；该部分按 spec 属于后续 Task 14/16，不能据此宣称已上线。 |

## 逐文件结论（对照 design.md）

| 文件 | 保留/修改/缺失 | 依据 |
|---|---|---|
| `dic-fe/src/api/data-query/index.ts` | 保留 | 三个浏览器 API 与 design 6.1 一致，复用共享 Axios，轮次 body 只有 `question`，支持 AbortSignal。 |
| `dic-fe/src/api/data-query/__tests__/index.spec.ts` | 保留 | 精确验证空会话 body、仅 question、ID 编码和查询取消 signal。 |
| `dic-fe/src/modules/data-query/types/index.ts` | 保留 | 页面视图状态与 API 依赖明确，局部停止原因不伪造成后端终态。 |
| `dic-fe/src/modules/data-query/constants/index.ts` | 修改 | 保留状态、上限和大部分固定安全文案；把 `DQ_COMPILATION_FAILED` 从 unsupported 改为 system，并补测试。 |
| `dic-fe/src/modules/data-query/composables/useDataQueryConversation.ts` | 保留 | 单活动轮询链、有界次数、终态停止、Abort 与旧回写隔离均完整。 |
| `dic-fe/src/modules/data-query/composables/__tests__/useDataQueryConversation.spec.ts` | 修改 | 现有 13 项轮询/清理/安全测试保留；补 `DQ_COMPILATION_FAILED → system` 分类断言。 |
| `dic-fe/src/modules/data-query/components/DataQueryComposer.vue` | 保留 | 非空/长度约束、忙态禁用、提交和停止等待交互符合页面职责。 |
| `dic-fe/src/modules/data-query/components/DataQueryTurnCard.vue` | 保留 | 显示问题、六态标签、安全错误、结果和最终回答。 |
| `dic-fe/src/modules/data-query/components/DataQueryResultTable.vue` | 保留 | 受控列行按 Vue 文本插值呈现，支持空结果与横向滚动。 |
| `dic-fe/src/modules/data-query/components/__tests__/DataQueryComponents.spec.ts` | 保留 | 覆盖输入、停止、六态展示、结果/回答和 XSS 文本转义。 |
| `dic-fe/src/views/data-query/Conversation.vue` | 保留 | 创建会话、消息列表、输入、路由离开/卸载清理组合完整。 |
| `dic-fe/src/views/data-query/__tests__/Conversation.spec.ts` | 保留 | 路由级 smoke、提交链、离开清理和轮询异常恢复均有验证。 |
| `dic-fe/src/router/modules/data-query.ts` | 保留 | 独立服务层路由和菜单元数据齐全。 |
| `dic-fe/src/router/__tests__/data-query.spec.ts` | 保留 | 验证路由可达、可见菜单和懒加载组件。 |
| `dic-fe/src/api/request.ts` | 保留（既有共享依赖） | data-query 正确复用其身份注入、统一解包和 401 登录重试。 |
| `dic-fe/src/router/index.ts`、`src/layouts/MainLayout.vue` | 保留（既有聚合机制） | glob 自动注册模块路由，菜单从 meta 聚合，无需为问数特改。 |
| `dic-fe/scripts/scan-data-query-chunks.mjs` | 保留 | 从 Vite manifest 递归扫描问数入口可达 JS，覆盖 AK/SK/JWT/Authorization/Cookie/raw SQL 且不回显命中值。 |
| `dic-fe/scripts/__tests__/scan-data-query-chunks.spec.mjs` | 保留 | 覆盖各敏感类别、共享请求 chunk 递归、无关页面排除和不回显。 |
| `dic-fe/package.json`、`vite.config.ts` | 保留 | 所有 build 命令串联产物扫描，manifest 为扫描提供真实 chunk 图。 |
| `dic-fe/Dockerfile`、`nginx.conf`、`.dockerignore`、`scripts/__tests__/container-artifacts.spec.mjs` | 保留（仅作为构建证据） | 多阶段锁定安装、test-mode 构建、非 root nginx、SPA base/health 静态测试通过；未执行镜像构建或部署健康检查。 |
| `dic-fe/.env.production`、`.env.test`、`.env.release`、`.env.development` | 保留 | production/test 只配置 dic-be 现有 API base，未配置 DSH/Broker/数据源；release/development 使用既有相对 API 回退。 |
| `dic-fe/README.md` | 保留 | 记录 production/test build 与容器服务方式；不作为运行成功的替代证据。 |
| `dic-fe/context.md` | 保留（辅助且不入镜像） | 仅记录本地开发交接，`.dockerignore` 排除；其中历史“Task12 定向 26 passed”已与本轮实跑 36 passed 不同，不作为评审证据。 |
| design 4.1 所需独立路由、页面、API Client | 无缺失 | 代码文件齐全；唯一需修改项为错误分类，不是缺失文件。 |

## 发现（按 P1/P2/P3）

### P1 阻断

无。

### P2 需修

1. **`DQ_COMPILATION_FAILED` 被误报为用户问法超能力。** `src/modules/data-query/constants/index.ts:33` 将该码映射到“当前问法暂不支持，请尝试单个数据集的汇总查询”；`design.md:376` 明确该码是治理映射无法安全编译，需修复治理记录。该错误应使用稳定的系统失败文案，避免误导用户反复改写问题。`useDataQueryConversation.spec.ts:39-44` 的表驱动用例覆盖 semantic/agent/timeout，却未锁定 compilation 分类。

### P3 建议

无。生产构建仅有既有依赖 chunk 大小和浏览器数据库陈旧警告，不影响本任务结论。

## 证据清单

- `git status --short --branch`（主仓）与 `git -C .\dic-fe status --short --branch`：两仓均在 `feat/controlled-data-query-mvp`；记录了评审前已有未提交工作，Task 12 实现全部位于独立 `dic-fe` 工作树。
- `git diff --stat`、`git diff -- README.md package.json vite.config.ts`、`git ls-files --others --exclude-standard`、相关 `git log`：确定 3 个 tracked 改动和 21 个 untracked 文件；相关 log 只命中 stash 引用，没有可替代当前工作树的 Task 12 提交。
- `pnpm exec vitest run src/api/data-query/__tests__/index.spec.ts src/modules/data-query/composables/__tests__/useDataQueryConversation.spec.ts src/modules/data-query/components/__tests__/DataQueryComponents.spec.ts src/views/data-query/__tests__/Conversation.spec.ts src/router/__tests__/data-query.spec.ts scripts/__tests__/scan-data-query-chunks.spec.mjs scripts/__tests__/container-artifacts.spec.mjs`：退出 0，7 个文件、36 个测试全部通过。
- `pnpm run type-check`：退出 0，`vue-tsc --noEmit` 无诊断。
- `pnpm exec eslint <Task 12 的 18 个 TS/Vue/MJS 文件及 vite.config.ts>`：退出 0，无 lint 输出；仅 stderr 提示 baseline-browser-mapping 数据陈旧。
- `pnpm run build`：退出 0，Vite production build 转换 2632 modules，生成 `dist/.vite/manifest.json` 和 `assets/Conversation-BddcG8Nv.js`，随后 `scan:data-query-chunks` 退出 0；构建警告 `giime`/`elementPlus` chunk 超过 500 kB，非失败。
- `pnpm run scan:data-query-chunks`：对实际 production `dist` 独立复跑，退出 0、零命中。
- 首次用于打印 manifest 的 PowerShell `ConvertFrom-Json -AsHashtable`/`??` 诊断命令退出 1 且无输出（shell 语法兼容问题）；随后等价 `node -e` 命令退出 0，确认问数入口文件和 `vue/request/giime/elementPlus` 四个静态 imports。
- 自定义 Node 只读扫描实际 manifest 可达的 5 个 JS 文件：退出 0，`internalBroker`、DSH 地址、身份字段和查询字段 findings 均为空。
- 限定源码 grep：data-query 生产源码未发现 `userId`、principal、role、datasetCode、SQL、Authorization、DSH 地址、AK/SK、存储或 cookie 字段；测试中的恶意示例不进入生产 chunk。
- `git -C .\dic-fe diff --check`：退出 0。
- 评审后再次执行两仓 `git status --short --branch`：除本报告外，前端实现仓状态与评审前相同；未修改实现、`tasks.md` 或历史报告。

## 待办

- [ ] 将 `src/modules/data-query/constants/index.ts` 中 `DQ_COMPILATION_FAILED` 映射为 `SAFE_ERROR_MESSAGES.system`，不要提示用户改写问法。
- [ ] 在 `useDataQueryConversation.spec.ts` 的错误分类用例中加入 `DQ_COMPILATION_FAILED → SAFE_ERROR_MESSAGES.system`，然后重跑本报告中的定向测试、type-check、scoped lint 和 production build/扫描。
- [ ] 修复后触发 Task 12 下一轮独立评审；Task 14/16 再提供真实容器/部署健康与端到端环境证据。
