# 任务 2 评审 — dic-be 语义治理领域模型（2026-08-25 r2）

**状态**：approved
**评审轮次**：r2
**实现范围**：独立复审 `dic-be` 当前未提交工作树相对 `aedc6a7450797ee00c410b802df39a05f05ed2b4` 的改动。当前 `HEAD` 仍为 `aedc6a7`；工作树累计 26 个已跟踪文件变更及多项后续任务未跟踪文件。本轮针对 r1 两个 P2 检查 `app/data_query/domain/policy.py`、`tests/test_data_query_policy.py`、`tests/test_data_query_service_mapping.py`（三文件相对基线 `+300/-103`），并沿用检查 `app/data_query/application/service.py` 的 ORM → 领域映射入口；其余 Task 3+ 会话、断言、执行器和部署改动不扩展为本轮 Task 2 结论。验证对象是 2026-08-25 当前工作树快照，不是新的提交。

## r1 两个 P2 复核

| r1 项目 | 本轮结果 | 证据 |
|---|---|---|
| P2-1 全部治理值闭合，非法值不得进入 `AuthorizedSemanticQuery` | **已修** | `policy.py` 为发布状态、数据源、指标聚合、维度类型、过滤操作符、策略主体类型、资源类型和 effect 定义 `Literal` 闭合集；`DatasetPolicy`、`MetricPolicy`、`DimensionPolicy`、`AccessPolicy`、`QueryScopeMapping` 的冻结构造器通过 `_require_governance_value` 做运行时成员校验。ORM 字段仍可使用字符串保存，但 `_dataset_policy`、`_metric_policy`、`_dimension_policy`、`_access_policy` 在进入领域对象时 fail-closed。独立对抗脚本覆盖 12 个领域构造入口和 1 个持久化映射入口，输出 `GOVERNANCE_CASES_REJECTED=13`；非法值均在产生授权快照前以 `DATASET_CONFIG_INVALID` 拒绝。 |
| P2-2 内部原因到 `DQ_POLICY_DENIED` / `DQ_SEMANTIC_INVALID` 的分类原则穷尽且有表驱动测试 | **已修** | `policy.py` 明确原则：身份/资源授权、MVP 数据源资格、权威范围缺失归策略拒绝；请求语义、未发布目录项、损坏治理配置归语义无效。两个集合在模块加载时检查互斥且并集等于 `QueryPolicyErrorCode` 全集，否则抛 `RuntimeError`。参数化测试逐项列出全部 15 个原因；独立枚举验证输出 `CLASSIFICATION_TOTAL=15; POLICY=6; SEMANTIC=9; EXHAUSTIVE=TRUE`。 |

## 验收核验（对照 requirements.md）

| 验收标准 | 结果 | 证据 |
|---|---|---|
| 4.1 数据集仅在发布后可查询 | **通过** | `GovernanceStatus` 仅为 `draft/published`，非法状态在领域构造时拒绝；授权只查找 `published`。 |
| 4.2 MVP 仅执行 MaxCompute | **通过** | `DataSourceType` 闭合为 `maxcompute/hologres`，授权仅接受 `maxcompute`，其他闭合集成员映射 `SOURCE_NOT_ALLOWED`。 |
| 4.3 指标、维度、过滤维度、排序字段必须已发布且属于数据集 | **通过** | r1 已验证逐资源查找；本轮闭合所有相关治理状态、聚合、类型和操作符，聚焦套件无回归。 |
| 4.4 主体必须有数据集及每个请求资源的显式 allow | **通过** | `_is_allowed` 保持默认拒绝；198 项聚焦测试通过。 |
| 4.5 allow/deny 同时匹配时 deny 优先 | **通过** | `test_resource_deny_overrides_allow` 等测试包含在通过套件。 |
| 4.6 支持用户和数据角色，未知主体不匹配 | **通过** | `PolicySubjectType` 仅允许 `user/data_role`；未知持久化值现在不能构造 `AccessPolicy`。 |
| 4.7 过期、草稿、禁用、未来策略不生效 | **通过** | 策略状态闭合，既有有效期和状态测试通过；非法状态不能进入匹配函数。 |
| 4.8 从权威资料生成不可移除范围 | **通过** | `QueryScopeMapping.op` 闭合；持久化 mapping 严格解析，权威 Staff 范围和 Broker `AND` 合并的既有测试通过。 |
| 4.9 发布所需治理配置完整 | **通过（Task 2 授权快照入口）** | 数据源、状态、聚合、维度类型/操作符及策略值在 ORM → 领域入口严格解析；物理标识符和执行上限由既有授权/编译测试验证。非法治理值不再延迟进入 `AuthorizedSemanticQuery`。实际发布 API 属后续任务，不在本轮宣称。 |
| 4.10 不根据现有资产自动开放权限 | **通过** | 本轮未新增 seed/自动 allow；默认拒绝测试继续通过。 |
| 5.1 至少一个指标 | **通过** | 空指标仍映射 `EMPTY_METRICS → DQ_SEMANTIC_INVALID`。 |
| 5.2 严格校验治理维度过滤 | **通过** | 操作符闭合；类型、数量、逆序 between、MaxCompute 日期时间格式测试通过。 |
| 5.3 治理时间范围和结束排他 | **通过** | 日期/日期时间范围测试通过；非法或不支持格式 fail-closed。 |
| 5.4 仅按已选择字段 asc/desc 排序 | **通过** | 非法方向和未选择字段分别稳定映射为语义无效。 |
| 5.5 `limit`、100 行和 30 秒限制 | **通过** | 数据集限制和请求 limit 的既有边界测试包含在 198 项通过结果。 |
| 5.6 禁止自由 SQL、多语句和写操作 | **通过（Task 2 编译路径）** | 聚合闭合为 `sum/count/avg/min/max`；聚焦 Broker 测试通过，物理表达式不能从非法治理聚合进入快照。 |
| 8.1 稳定且可分类的错误码 | **通过** | 15 个内部原因被互斥、穷尽地分类到两种 Task 2 外部码，参数化测试和独立枚举验证均通过；HTTP 传播测试也在 198 项套件中通过。 |

## 逐文件结论（对照 design.md）

| 文件 | 保留/修改/缺失 | 依据 |
|---|---|---|
| `app/data_query/domain/policy.py` | **保留** | 新增八类治理 `Literal`、运行时闭合校验、冻结值对象入口、互斥穷尽的错误分类集合及加载期不变量，满足 design 8、9、10.2。 |
| `app/data_query/application/service.py` | **保留** | 本轮相关 ORM → 领域映射继续统一经过已加固的领域构造器；`_dimension_policy` 和 scope parser 另有显式严格解析。该文件相对基线的其余会话/审计变更属后续任务。 |
| `app/data_query/domain/models.py` | **保留** | SQLAlchemy 字段保持字符串是持久化表示；严格校验位于持久化边界后的领域映射和冻结领域对象，未再让开放值进入授权快照。 |
| `tests/test_data_query_policy.py` | **保留** | `test_closed_governance_values_cannot_enter_authorized_snapshot` 覆盖全部遗留治理类别；`test_internal_reason_has_stable_external_classification` 以 15 行参数表固定分类。 |
| `tests/test_data_query_service_mapping.py` | **保留** | 新增持久化开放字符串全类别拒绝测试；既有测试继续覆盖非法维度类型和 operators JSON。 |
| Task 2 闭合治理值定义/解析 | **已补齐** | r1 标为“缺失”的统一闭合集和严格入口现已存在，无缺失文件。 |

## 发现（按 P1/P2/P3）

### P1 阻断

无。

### P2 需修

无。r1 的两个 P2 均已通过当前工作树代码、表驱动测试及独立对抗验证。

### P3 建议

无。本轮仅记录一个非阻断环境告警：FastAPI `TestClient` 触发 1 个 Starlette/httpx 弃用告警，不影响 Task 2 结论。

## 证据清单

### 1. 基线、当前工作树和差异范围

```powershell
cd E:\dataworks_agent\dic-be
git status --short --branch
git rev-parse HEAD
git rev-parse aedc6a7
git log -5 --oneline --decorate
git diff --stat aedc6a7
git diff --name-status aedc6a7
git diff --numstat aedc6a7 -- app/data_query/domain/policy.py tests/test_data_query_policy.py tests/test_data_query_service_mapping.py
git diff --check aedc6a7
```

关键结果：

```text
branch: feat/controlled-data-query-mvp
HEAD=aedc6a7450797ee00c410b802df39a05f05ed2b4
baseline=aedc6a7450797ee00c410b802df39a05f05ed2b4
26 tracked files changed, 2832 insertions(+), 480 deletions(-)
P2 direct files:
132  38  app/data_query/domain/policy.py
137  65  tests/test_data_query_policy.py
31   0   tests/test_data_query_service_mapping.py
DIFF_CHECK_EXIT=0
```

当前工作树另有后续任务未跟踪文件；本轮没有把它们当作两个 P2 的实现证据。

### 2. Task 2 聚焦测试

```powershell
python -m pytest -q `
  tests/test_data_query_assertion.py `
  tests/test_data_query_audit.py `
  tests/test_data_query_broker.py `
  tests/test_data_query_internal_schema.py `
  tests/test_data_query_models.py `
  tests/test_data_query_policy.py `
  tests/test_data_query_schemas.py `
  tests/test_data_query_service_mapping.py `
  tests/test_response.py
```

```text
198 passed, 1 warning in 1.83s
exit code: 0
```

### 3. 非法治理值独立对抗验证

通过 stdin Python 脚本逐一构造非法 `dataset.source_type/status`、`metric.status/aggregation`、`dimension.status/type/operators`、`policy.subject_type/resource_type/effect/status`、`scope.operator`，并额外将非法持久化 metric aggregation 送入 `service._metric_policy`。

```text
REJECTED dataset.source_type -> DATASET_CONFIG_INVALID
REJECTED dataset.status -> DATASET_CONFIG_INVALID
REJECTED metric.status -> DATASET_CONFIG_INVALID
REJECTED metric.aggregation -> DATASET_CONFIG_INVALID
REJECTED dimension.status -> DATASET_CONFIG_INVALID
REJECTED dimension.type -> DATASET_CONFIG_INVALID
REJECTED dimension.operators -> DATASET_CONFIG_INVALID
REJECTED policy.subject_type -> DATASET_CONFIG_INVALID
REJECTED policy.resource_type -> DATASET_CONFIG_INVALID
REJECTED policy.effect -> DATASET_CONFIG_INVALID
REJECTED policy.status -> DATASET_CONFIG_INVALID
REJECTED scope.operator -> DATASET_CONFIG_INVALID
REJECTED persisted metric.aggregation -> DATASET_CONFIG_INVALID
GOVERNANCE_CASES_REJECTED=13
exit code: 0
```

这些值均在领域对象/持久化映射入口被拒绝；`authorize_semantic_query` 只能把已验证的冻结 `DatasetPolicy`、`MetricPolicy`、`DimensionPolicy` 和引用维度写入 `AuthorizedSemanticQuery`。

### 4. 错误分类独立穷尽验证

通过 stdin Python 脚本枚举 `QueryPolicyErrorCode`，对每个成员构造 `QueryPolicyError`，断言外部码仅属于规定的两类，并断言 policy 集精确等于六个约定原因。

```text
DATASET_NOT_PUBLISHED -> DQ_SEMANTIC_INVALID
ACCESS_DENIED -> DQ_POLICY_DENIED
METRIC_NOT_ALLOWED -> DQ_POLICY_DENIED
DIMENSION_NOT_ALLOWED -> DQ_POLICY_DENIED
SOURCE_NOT_ALLOWED -> DQ_POLICY_DENIED
RESULT_LIMIT_EXCEEDED -> DQ_SEMANTIC_INVALID
FILTER_DIMENSION_NOT_ALLOWED -> DQ_POLICY_DENIED
FILTER_INVALID -> DQ_SEMANTIC_INVALID
DUPLICATE_FIELD -> DQ_SEMANTIC_INVALID
EMPTY_METRICS -> DQ_SEMANTIC_INVALID
DATASET_CONFIG_INVALID -> DQ_SEMANTIC_INVALID
SCOPE_REQUIRED_MISSING -> DQ_POLICY_DENIED
TIME_RANGE_NOT_ALLOWED -> DQ_SEMANTIC_INVALID
ORDER_BY_FIELD_NOT_SELECTED -> DQ_SEMANTIC_INVALID
ORDER_BY_INVALID -> DQ_SEMANTIC_INVALID
CLASSIFICATION_TOTAL=15; POLICY=6; SEMANTIC=9; EXHAUSTIVE=TRUE
exit code: 0
```

实现中的加载期断言还保证两个集合不得重叠，且后续新增内部枚举成员而未分类时模块立即失败。

### 5. 编译检查

```powershell
python -m compileall -q app
```

```text
COMPILEALL_EXIT=0
```

## 待办

无 Task 2 P1/P2 待办。后续提交或继续修改该未提交工作树后，如 Task 2 相关三文件再次变化，应基于新快照重新评审。
