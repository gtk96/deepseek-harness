# 任务 12 评审 — 实现 dic-fe 智能问数页面

**状态**：approved
**评审轮次**：r2
**实现范围**：独立仓库 `dic-fe/` 当前未提交的 Task 12 工作树，重点复审 `src/modules/data-query/constants/index.ts` 与 `src/modules/data-query/composables/__tests__/useDataQueryConversation.spec.ts` 对 r1 唯一 P2 的修复；同时重新检查 `src/api/data-query/**`、`src/modules/data-query/**`、`src/views/data-query/**`、`src/router/modules/data-query.ts`、`src/router/__tests__/data-query.spec.ts`、`scripts/scan-data-query-chunks.mjs` 及测试、容器静态测试、`package.json` 和 `vite.config.ts`，并复跑 Task 12 定向验证。主仓与 `dic-fe` 均位于 `feat/controlled-data-query-mvp`；实现仍是未提交工作树，相关提交搜索只命中既有 stash 引用。本轮仅新建此报告，不修改实现、`tasks.md` 或历史报告。

## 验收核验（对照 requirements.md）

| 验收标准 | 结果 | 证据 |
|---|---|---|
| 12.1 / 需求 1.1–1.2、2.1–2.7：API Client 只向 dic-be 创建会话、创建轮次和查询轮次，轮次 body 仅有 `question`，复用共享身份和 401 处理 | 通过 | `src/api/data-query/__tests__/index.spec.ts` 3 项通过；定向套件继续验证空会话 body、轮次 body 精确键、ID 编码及 AbortSignal。相关生产代码纳入 type-check 和 scoped lint，均退出 0。 |
| 12.2 / 需求 2.1–2.7：独立路由、菜单元数据、会话消息、输入、排队/运行、结果表格、最终回答和全部终态 | 通过 | 路由测试 2 项、组件测试 9 项、页面测试 2 项全部通过；production build 生成 manifest 中的动态入口 `assets/Conversation-BpZ5oteD.js`。 |
| 12.3 / 需求 2.5–2.7：有界轮询，在全部终态、离开页面、取消和卸载时停止 | 通过 | composable 测试 14 项与页面测试全部通过，覆盖 queued/running/succeeded、denied/failed/timed_out、最大次数、停止、销毁、Abort 和旧响应隔离。 |
| 12.3 / 需求 8.2、8.5–8.6：权限不足、能力不支持、超时和系统失败分类准确，不展示内部细节 | 通过 | `ERROR_CODE_MESSAGES` 当前将 `DQ_POLICY_DENIED`、`DQ_SEMANTIC_INVALID`、`DQ_QUERY_TIMEOUT` 分别映射为 permission、unsupported、timeout；未知/Agent/网络失败使用 system。含 SQL、Authorization、stack 的异常安全文案测试通过。 |
| r1 P2：`DQ_COMPILATION_FAILED` 必须映射固定系统失败文案 | 通过 | `src/modules/data-query/constants/index.ts` 当前为 `DQ_COMPILATION_FAILED: SAFE_ERROR_MESSAGES.system`，其固定文案为“查询暂时不可用，请稍后重试。”，不再提示用户改写问法，符合 `design.md` 10.2 对“治理映射无法安全编译、修复治理记录”的定义。 |
| r1 P2：必须有 `DQ_COMPILATION_FAILED → system` 回归测试 | 通过 | `useDataQueryConversation.spec.ts` 的终态表新增 `['failed', 'DQ_COMPILATION_FAILED', SAFE_ERROR_MESSAGES.system]`；本轮实跑该文件 14 项通过，总定向套件 7 files / 37 tests 通过，比 r1 增加 1 项回归用例。 |
| 12.4 / 需求 2.2–2.7、10.8：API body、轮询清理、全部终态、路由、类型、lint 和 production build/产物扫描 | 通过 | 定向 Vitest、`vue-tsc --noEmit`、18 文件 scoped ESLint、`vite build` 内置扫描及独立复扫全部退出 0；构建转换 2632 modules，并生成问数入口和 manifest。 |
| 需求 1.7、10.8 的 dic-fe 构建部分 | 通过；真实部署健康仍不在 Task 12 验证范围 | production build 和问数入口递归敏感内容扫描退出 0、零命中。构建仅报告既有浏览器数据库陈旧和 `giime`/`elementPlus` 大 chunk 警告，不构成 Task 12 P1/P2。实际容器部署与 `/healthz` 属于后续任务，本轮未据静态测试宣称上线。 |

## 逐文件结论（对照 design.md）

| 文件 | 保留/修改/缺失 | 依据 |
|---|---|---|
| `dic-fe/src/modules/data-query/constants/index.ts` | 保留（r1 要求的修改已完成） | `DQ_COMPILATION_FAILED` 已映射到唯一固定系统失败文案；其他稳定错误分类和终态集合保持一致。 |
| `dic-fe/src/modules/data-query/composables/__tests__/useDataQueryConversation.spec.ts` | 保留（r1 要求的测试已补齐） | 新增 compilation failure 表驱动回归项，实跑 14 项通过；同时保留轮询、清理和内部细节隐藏覆盖。 |
| `dic-fe/src/modules/data-query/composables/useDataQueryConversation.ts` | 保留 | 固定受限轮询、终态停止、Abort、版本隔离和固定错误投影继续通过定向测试、类型检查与 lint。 |
| `dic-fe/src/api/data-query/index.ts`、`src/api/data-query/__tests__/index.spec.ts` | 保留 | 三接口、仅 question、共享请求实例和取消信号的 3 项 API 测试通过。 |
| `dic-fe/src/modules/data-query/types/index.ts` | 保留 | 视图状态与局部停止原因类型通过 `vue-tsc`。 |
| `dic-fe/src/modules/data-query/components/*.vue`、`components/__tests__/DataQueryComponents.spec.ts` | 保留 | 输入、停止、六态、结果/回答和文本安全的 9 项测试通过。 |
| `dic-fe/src/views/data-query/Conversation.vue`、`views/data-query/__tests__/Conversation.spec.ts` | 保留 | 提交链和页面离开清理 2 项测试通过，且 production 动态入口成功产出。 |
| `dic-fe/src/router/modules/data-query.ts`、`src/router/__tests__/data-query.spec.ts` | 保留 | 独立路由、菜单元数据和懒加载入口 2 项测试通过。 |
| `dic-fe/scripts/scan-data-query-chunks.mjs`、`scripts/__tests__/scan-data-query-chunks.spec.mjs` | 保留 | 扫描器 5 项测试通过；build 内置扫描和独立 production `dist` 复扫均退出 0。 |
| `dic-fe/Dockerfile`、`nginx.conf`、`.dockerignore`、`scripts/__tests__/container-artifacts.spec.mjs` | 保留（构建辅助证据） | 容器静态测试 2 项通过；未将其扩张为真实部署证据。 |
| `dic-fe/package.json`、`vite.config.ts` | 保留 | production build 串联问数产物扫描并生成 Vite manifest；类型、lint 和构建均通过。 |
| design 4.1 / 13.3 的独立 API、路由、页面、错误映射和测试文件 | 无缺失 | 7 个定向文件 37 项全部通过；r1 指出的两个修改点均已落实，未发现新增缺失。 |

## 发现（按 P1/P2/P3）

### P1 阻断

无。

### P2 需修

无。r1 唯一 P2 已修复并由新增回归测试锁定；本轮代码检查与复跑未发现新增 P1/P2。

### P3 建议

无。浏览器兼容数据陈旧与现有依赖 chunk 大小仅为非阻断警告，不属于本任务新增问题。

## 证据清单

- 读取 `.kiro/reviews/task12-2026-08-25-r1.md`：确认上一轮仅有一个 P2，即 compilation failure 被误分为 unsupported 且缺少回归测试。
- `git status --short --branch`（主仓）、`git -C .\dic-fe status --short --branch`、两仓 diff stat 和相关 log：退出 0；确认两仓分支、既有未提交范围及无可替代当前工作树的 Task 12 正式提交。
- 源码检查：`src/modules/data-query/constants/index.ts` 显示 `DQ_COMPILATION_FAILED: SAFE_ERROR_MESSAGES.system`；`useDataQueryConversation.spec.ts` 显示对应 `failed → system` 表驱动断言。
- `pnpm exec vitest run src/api/data-query/__tests__/index.spec.ts src/modules/data-query/composables/__tests__/useDataQueryConversation.spec.ts src/modules/data-query/components/__tests__/DataQueryComponents.spec.ts src/views/data-query/__tests__/Conversation.spec.ts src/router/__tests__/data-query.spec.ts scripts/__tests__/scan-data-query-chunks.spec.mjs scripts/__tests__/container-artifacts.spec.mjs`：退出 0，7 个文件、37 项测试全部通过；composable 文件 14 项通过。
- `pnpm run type-check`：退出 0，`vue-tsc --noEmit` 无诊断。
- `pnpm exec eslint <Task 12 的 18 个 TS/Vue/MJS 文件及 vite.config.ts>`：退出 0，无 lint 诊断；仅报告 `baseline-browser-mapping` 数据陈旧提示。
- `pnpm run build`：退出 0，Vite production build 转换 2632 modules，生成 `dist/.vite/manifest.json` 和 `assets/Conversation-BpZ5oteD.js`；命令串联的 `scan:data-query-chunks` 退出 0。仅有 browserslist 数据陈旧和两个依赖 chunk 超过 500 kB 的警告。
- `pnpm run scan:data-query-chunks`：对实际 production `dist` 独立复跑，退出 0、零命中。
- manifest 只读检查：`src/views/data-query/Conversation.vue` 是动态入口，文件为 `assets/Conversation-BpZ5oteD.js`，静态 imports 为 vue、request、giime、elementPlus chunks。
- `git -C .\dic-fe diff --check`：退出 0。
- 写报告前受保护文件 SHA-256：`tasks.md` 为 `3374F4BD46E88004AB0395279289E00AF36CE611C328201150F625857D9F9A07`，r1 为 `D3B52D48D9598E199BF03C54812FC32B85D3CADF085267E70DEDAFBA23DD01A7`；写后复核相同。本轮开始时同日 Task 12 仅有 r1，因此新报告轮次为 r2。

## 待办

无。Task 12 在本轮验证范围内可批准；后续真实容器/部署健康和环境端到端验收仍按 Task 14/16 提供证据。
